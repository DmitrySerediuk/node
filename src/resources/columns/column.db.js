const Column = require('./column.model');

const columnDb = [
  new Column({ id: '1', title: 'board1', order: '1' }),
  new Column({ id: '2', title: 'board1', order: '2' })
];

module.exports = columnDb;
